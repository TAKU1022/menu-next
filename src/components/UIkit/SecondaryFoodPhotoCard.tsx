import { VFC } from 'react';
import { Food } from '@/types/typeFood';

type Props = {
  foodData: Food;
};

export const SecondaryFoodPhotoCard: VFC<Props> = ({ foodData }) => {
  return <div></div>;
};
